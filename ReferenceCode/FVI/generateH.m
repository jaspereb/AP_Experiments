function [H] = generateH(x,y)
%GENERATEH Take the current sensor state and a fixed target state, generate the
%linearisation of the observation function around the current sensor state. 
H = eye(3);
end

